{
  "private": true,
  "scripts": {
    "clean": "rm -rf node_modules coverage c.out temp yarn.lock",
    "setup": "yarn clean && yarn install"
  },
  "devDependencies": {
    "prettier": "2.6.0",
    "prettier-plugin-go-template": "0.0.11"
  }
}
